apiVersion: v1
kind: Service
metadata:
  name: {{ include "cna.fullname" . }}
  labels:
    {{- include "cna.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "cna.selectorLabels" . | nindent 4 }}
